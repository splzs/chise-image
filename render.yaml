services:
  - type: web
    name: qishi-image
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: IMAGE_MODEL
        value: gpt-image-2
      - key: SESSION_SECRET
        sync: false
